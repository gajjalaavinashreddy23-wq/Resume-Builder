# Copy this file to `.env` and set the real values (DO NOT commit `.env`)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xhzxm6g.mongodb.net/resume-builder?retryWrites=true&w=majority
# Clerk secret (if used)
CLERK_SECRET_KEY=
# Path to pdflatex binary (optional)
PDFLATEX_PATH=
# Frontend: show hero video (set to 'true' to display)
VITE_SHOW_VIDEO=false
